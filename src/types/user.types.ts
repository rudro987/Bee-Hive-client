export type TLoginInputs = {
    email: string;
    password: string;
  };

export type TUserTypes = {
    _id: string;
    name: string;
    email: string;
    role: string;
    phone: string;
    address: string;
}